# v0.5.0 导出实现计划

**目标：** 增加当前文档的单文件 HTML 和 PDF 导出并发布安装包。

**架构：** 渲染进程复用现有 Markdown、图片和 Mermaid 渲染；主进程封装独立 HTML 页面及 PDF 打印。两种格式共用渲染结果。

**技术栈：** React、Electron、KaTeX、Mermaid、GitHub Actions。

**规格：** `docs/superpowers/specs/2026-09-25-v0.5.0-export-design.md`。

## 步骤

- [x] 增加渲染进程导出内容构建，处理当前文本、本地图片和 Mermaid。
- [x] 增加主进程导出服务、HTML 样式与公式字体、PDF 隐藏窗口、文件写入。
- [x] 在 IPC 类型、预加载与界面添加 HTML/PDF 导出入口及错误反馈。
- [x] 更新版本号、使用文档、路线图与 macOS 临时签名发布条件。
- [ ] 检查构建与类型，审查差异，提交并推送 main。
- [ ] 推送 v0.5.0 标签，等待发布工作流产出安装包和校验文件。
